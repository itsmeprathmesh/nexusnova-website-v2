'use client';
import { useState } from 'react';import { Send } from 'lucide-react';
const fields=[['name','Name'],['business_name','Business name'],['phone','Phone / WhatsApp'],['email','Email'],['service_needed','Service needed'],['budget','Budget range'],['timeline','Timeline']];
export function LeadForm(){const [loading,setLoading]=useState(false);const [done,setDone]=useState(false);const [error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const data=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});setLoading(false);if(res.ok){setDone(true);(e.target as HTMLFormElement).reset()}else setError('Something went wrong. Please try again or WhatsApp us.')}return <form onSubmit={submit} className="glass-warm rounded-[2rem] p-6 md:p-8"><div className="grid gap-4 md:grid-cols-2">{fields.map(([n,l])=><label key={n} className="text-sm text-amber-50/62">{l}<input required name={n} type={n==='email'?'email':'text'} className="mt-2 w-full rounded-2xl border border-amber-200/15 bg-white/[.06] px-4 py-3 text-amber-50 outline-none focus:border-amber-300"/></label>)}</div><label className="mt-4 block text-sm text-amber-50/62">Project message<textarea required name="message" rows={5} className="mt-2 w-full rounded-2xl border border-amber-200/15 bg-white/[.06] px-4 py-3 text-amber-50 outline-none focus:border-amber-300"/></label>{error&&<p className="mt-4 text-red-300">{error}</p>}{done&&<p className="mt-4 rounded-2xl bg-green-500/10 p-4 text-green-200">Inquiry received. We also sent you a confirmation email.</p>}<button disabled={loading} className="mt-5 flex w-full items-center justify-center gap-2 btn-lux px-6 py-4 disabled:opacity-60">{loading?'Sending...':'Send Inquiry'}<Send size={18}/></button></form>}
